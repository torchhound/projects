#import <stdio.h>

int initArray(int n){ //realloc for dynamic, n as func arg?
	int arrX[n], arrY[n], arrZ[n], arrI[n]; //3 dimensions and index array
	memset(arr, 0, sizeof(arrX, arrY, arrZ)); //3d array
}

